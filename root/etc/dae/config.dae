global {
    log_level: warn
    lan_interface: br-lan
    wan_interface: auto
    auto_config_kernel_parameter: true
}

# subscription {
#     mysub: 'https://your-subscription-url'
# }

# group {
#     proxy {
#         filter: subtag(mysub)
#         policy: min_moving_avg
#     }
# }

routing {
    dip(224.0.0.0/3, 'ff00::/8') -> direct(must)
    dip(geoip:private)            -> direct(must)

    fallback: direct
}
